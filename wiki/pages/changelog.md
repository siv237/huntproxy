---
updated: 2026-09-17
commit: b028d67
tags: [analysis]
---

# История разработки (восстановленный changelog по git)

Реконструкция по `git log`. Всего **269 коммитов** (2026-06-06 … 2026-09-17),
основной автор — Sergey Ivanov (два адреса), плюс несколько коммитов от root.
Ниже — хронология по месяцам с ключевыми коммитами; `hash` — реальные
идентификаторы для `git show`.

Страница — конспект вех, а не копия `git log`: сырую историю при необходимости
агент получает напрямую из git, здесь фиксируется только синтез и значимые
решения. Новые записи добавляются по мере развития проекта (операция ingest,
см. схему в `AGENTS.md`).

## 2026-06 — рождение проекта (176 коммитов)

Начало (`ce0e98f` «вебка») — проект называется ещё `setproxy`.

- **Веб-UI:** `88259fc` новый SPA-дашборд (sidebar, темы, сортировки),
  `44f629e` разделение Overview и Hunt, `f6820a3` редизайн Overview.
- **Хранение:** `7b04b1d` перевод хранения на SQLite, `d94bfe0` перенос всего
  состояния из файлов в SQLite, `1805951` разнесение статистики и списков на
  разные базы, `90e083d` DB-backed downloads + backup/restore.
- **Прокси-серверы:** `24172ab` SOCKS5-сервер (порт 17278) + UI,
  `9b3bea1` единая карточка Proxy Server (HTTP+SOCKS5), `d1a5683` 407 Proxy Auth
  retry, `e1d12ce` transparent runner (SO_ORIGINAL_DST + iptables REDIRECT).
- **Маршрутизация:** `fbffbcf` маршрутизация по доменам на SQLite,
  `bf79236` кастомные прокси + интеграция с Routes, `6c61a1b` матчинг поддоменов.
- **Устойчивость:** `3d6f2db` connectivity-мониторинг с canary-хостами,
  `28fa4ce`/`bd16cf8` пауза/продолжение hunt при отвале интернета.
- **Данные:** `329e3ba` IP-блэклисты (загрузка, проверка egress, UI),
  `8888f12` система страновых блоклистов, `a6165aa` heatmap,
  `b855a09` полноценная поддержка SSL-прокси.
- **UI/локали:** `08cfae7` i18n (en/ru) и замена строк через `t()`,
  `f577308` grace-период для проверенных прокси, `af01ec6` Traffic Monitor.
- **Рефакторинг:** `8e9d926` разбиение `hunt.py` на функциональный пакет
  `hunt/`, `9fb25cd` переименование `setproxy → huntproxy`.
- **Качество:** `cbd5e07` первые pytest-тесты, `6154c9a` `test.sh`,
  `e025cdb` pre-commit hook + `install-hooks.sh`, `97beea8` расширение покрытия.
- **Установка:** `e17682a` однострочный curl|bash installer, `e3aa2ab` установка
  в `/opt` + systemd, `1255f5e` venv+PyYAML на Ubuntu 24.04.

## 2026-07 — рефакторинг, качество, антифрод (59 коммитов)

- **Большой рефакторинг (01.07):** `6fb88e3` spliting `server.py` на 8 доменных
  handler-модулей, `1401912` выделение `Router`, `6b897fe` `TaskExecutor`,
  `c383432` split `checking.py`/`health.py`, `f661b69` split `scheduler.py`,
  `5ede8c2` split `proxy_runner.py`/`state.py`, `a542222` все функции CC≤15.
- **Качество:** `0311e8a` ruff + branch coverage + silent-except,
  `74b64ae` устранение `except: pass`, `330ce90` security-тесты (bandit +
  pip-audit + hypothesis), `2f8b791` устранение ruff-нарушений, `7503411` ESLint.
- **Инструменты:** `bedf591` живая карта модулей `MODULES.md` + `module_map.py`,
  `759...` (см. `cb5d5ff` обновление AGENTS.md).
- **Прозрачный перехват:** `99efc3e` перехват всего трафика машины (iptables +
  вкладка), `0387835` one-click web-controlled, `b3803f7` split interception.
- **Производительность:** `c6b6611` кэш dashboard-ручек, `4f6d7ec` снижение CPU
  на старте, `e71a6cf` лёгкий `/api/proxy/alive`, `1e861c2` инкрементальное
  сохранение рейтингов, `ba61f3c` лимит параллелизма замеров скорости,
  `4ac7836` event loop не блокируется.
- **История переключений:** `dfb4082` таблица истории, `b63613c` извлечение
  модуля + точный SQL, `ca02fe8` переключения на pool-странице,
  `9f8f297` мини-история в карточке.
- **Антифрод:** `9cbb1db` fraud-поля `ProxyRating`, `5e8b113` POST
  `/api/proxy/fraud`, `719f088` UI-колонка Fraud, `b44ba22` proxycheck.io 0-100,
  `7b5623c` рейтинг v2 (EWMA, множители), `8496c53` MITM multi-target TLS,
  `a47f006` неподтверждённый фрод топит рейтинг, `86b237b` антифрод v3
  (ip-api как единственный источник, failcheck, буст за чистоту),
  `a431b5a` синхронизация вики с v3.
- **UI:** `5815e8b` reverse-DNS у клиентов + живой фильтр, `596a1df` строгий
  режим без фолбэка на пул, `907b4ba` фильтр по всему traffic_log.
- **PAC:** `b42b3a9` модуль автонастройки прокси + редактор исключений,
  `cb029a0` выбор IP интерфейсов.
- **Планировщик:** `3308e6c` перепись шедулера (очередь, last_ok, countdown),
  `024603d` scheduled fresh proxy intake.
- **Прочее:** `04fd403` fix billing traffic diagram, `716597d`/`0597133`
  RU domestic whitelist + split pool selected/best, `7a81ce5` i18n-тест против
  кириллицы, `204986c` commit-based version badge, `26b9d7a`/`d811f0b`
  clean-install vs update + `uninstall.sh`.

## 2026-08 — антифрод, производительность, PAC (30 коммитов)

- **02.08:** `024603d` свежий intake прокси, `df3979d` untrack `config.yaml`,
  `aaa290b` fix status poll/JS cache.
- **22.08:** `9cbb1db`, `5e8b113`, `719f088` — fraud-рейтинг и UI.
- **24.08:** `8496c53` MITM-детектор, `7b5623c` рейтинг v2, `4ac7836` perf,
  `a08099f` разбор рейтинга v2 в карточке, `a47f006` антифрод-окно свежести.
- **25.08:** `b4d1bf9` fix зависания UI, `7e08022`/`04b3fab`/`7e64c8a`/`ca68131`
  серия perf (SQL-ручки, TTL-кэш snapshot, бандл страниц, prefetch локалей,
  SWR-карточки), `a431b5a` доки вики, `bb022b8` редизайн Client Control.
- **26.08:** `09bf7fb` тип входа и geo в маршрутах, `5815e8b` reverse-DNS +
  живой фильтр, `596a1df` строгий routing + бейдж, `907b4ba` фильтр traffic_log.
- **28.08:** `b42b3a9` PAC + редактор, `cb029a0` выбор IP интерфейсов.
- **31.08:** `680b249` `db_maintenance` + фикс блокировок, `118e07d` живой пинг
  прокси (бейдж со спарклайном), `6089623` fail-fast ретраи + `update.sh`.

## 2026-09 — стабилизация (4 коммита)

- `13d36f6` — мёртвые/спуффинг-прокси больше не попадают в пул.
- `62f42ba` — «fastcommit».
- `f18b8b5` — MITM-детектор: свежее соединение на каждую цель (иначе порог
  `reached>=2` не набирался и MITM-прокси не помечались).
- `b028d67` — `.gitignore`: runtime-каталоги `dataN/` (текущий HEAD).

## Ключевые архитектурные вехи

1. `7b04b1d`/`d94bfe0`/`1805951` — переход на SQLite и разделение баз.
2. `8e9d926` — разбиение монолита `hunt.py` на пакет mixin-модулей.
3. `6fb88e3`/`1401912`/`6b897fe`/`c383432`/`f661b69` — декомпозиция server,
   scheduler, checking, health.
4. `7b5623c` + `86b237b` — рейтинг v2 и антифрод v3 (текущая формула).
5. `3308e6c` + `680b249` — единый планировщик и обслуживание БД.
6. `99efc3e` — прозрачный перехват всего трафика.
