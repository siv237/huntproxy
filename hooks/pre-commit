#!/usr/bin/env bash
set -euo pipefail

cd "$(dirname "$0")/../../"

# Run the full test suite before allowing the commit.
echo "Running tests before commit..."
#./test.sh

echo "Tests passed. Proceeding with commit."
