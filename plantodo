1. [DONE] При запуске перепроверять прокси из основного листа если им уже больше часа.
   - Добавлена `_revalidate_stale_proxies()` в `hunt/health.py`.
   - При запуске в `hunt/main.py` запускается фоновая задача, которая перепроверяет прокси из `working.txt` (если файл старше часа) и любые alive-прокси с `last_check` старше часа.

2. [DONE] Решить проблемы с определением скорости через ssl.
   - В `hunt/checking.py` добавлена `_https_speed_single()`.
   - Для SSL-прокси теперь делается CONNECT к целевому хосту `:443`, туннель апгрейдится через `start_tls`, и замеряется HTTPS-скорость (раньше использовался HTTP :80 и не работал с HTTPS-редиректами).

3. [DONE] Разобраться со списками проксей не везде влезает адаптировать динамически ширину.
   - В `web/css/components.css` `.table` перешла на `width: auto; min-width: 100%; table-layout: auto`.
   - В `web/js/components.js` ширины столбцов теперь задаются как `min-width`, а не фиксированный `width`, таблицы адаптируются к контейнеру с горизонтальным скроллом при необходимости.

4. [DONE] Решить наконец с правильностью отображения SSL-прокси: они обычно пропускают через себя HTTP, а у нас все отмечены как без HTTPS.
   - Бэкенд: `_update_rating()` теперь ставит `protocol = "https"` для прокси с `ssl_supported=True` (если это не SOCKS).
   - Фронтенд: `web/js/pages/proxy-pool.js` — `proxyUrl()` и `proxyProtoGroup()` теперь считают `ssl_supported` признаком HTTPS, формируют URL `https://` и бейдж HTTPS.

5. [DONE] В Client Log показывать не только куда, а через что — цепочку выхода через какой прокси или прямой с учетом роутинга.
   - Бэкенд: `_connect_by_route()` в `hunt/proxy_runner.py` теперь собирает `chain` (`direct`, `custom:...`, `proxy:...`, `pool:...`).
   - Логи записывают цепочку (например, `custom:foo (disabled) → direct`).
   - Фронтенд: `web/js/pages/proxy-pool.js` и `web/js/pages/proxy-control.js` теперь всегда показывают upstream/цепочку, включая `direct`.

---
Дополнительно (исправленные критические баги из аудита):
- Разделены ручной и скачанный IP-блэклист: `in_blacklist` в `to_dict()` только для ручного; IP-блэклист не исключает прокси из alive/working.txt, а только снижает score.
- `blacklist_remove()` не сбрасывает IP-блэклист, переприменяет проверку egress IP.
- Добавлено сохранение состояния в `_update_rating()` (каждые 25 обновлений), `proxy_runner`/`socks5_runner` start/stop и API `/api/proxy/start|stop`/`/api/settings/country_filter`.
- Маршрут `proxy:<addr>` корректно отказывается работать с ручно заблокированными прокси.
- Исправлена загрузка legacy-состояния и `blacklist.txt` (сохраняются/восстанавливаются `in_blacklist` и `blacklist_reason`).
- Исправлен `hunt.sh`:
  - Убран широкий `pgrep`, который убивал любой Python-процесс с `hunt.py` в командной строке.
  - По умолчанию `./hunt.sh` больше не убивает ничего; если порт занят, выводит предупреждение и завершается.
  - Убийство предыдущего процесса только по явному флагу `./hunt.sh --kill` (или `-K`).
  - Для поиска процесса на порту теперь сначала используется `ss -ltnp` (только слушатели), `lsof` — фолбэк.
  - Перед `exec` записывается `.hunt.pid`.
- В `hunt/main.py` возвращен `import argparse`, который отсутствовал и ломал запуск.
- Исправлена работа HTTP-прокси через `hunt/proxy_runner.py`:
  - Прокси, которые не поддерживают `CONNECT` (`supports_connect=false`), теперь корректно используются для HTTP-запросов: запрос передается напрямую с полным URL (`GET http://target/...`), а не пытается создать туннель.
  - Для HTTPS-запросов через такие прокси возвращается 502 (корректное поведение, т.к. без CONNECT HTTPS невозможен).
  - Добавлен параметр `need_connect` в `_connect_by_route()` и `_connect_upstream()`; для HTTP-форвардинга он `False`, для HTTPS/SOCKS — `True`.
  - Исправлена ошибка в `_handle_http_forward()`: заголовки клиента теперь читаются и передаются upstream всегда, а не только для относительных URL.
  - Добавлен тест `test_connect_by_route_http_non_connect_returns_raw`.
- Все тесты проходят: 142 passed.
