1. [DONE] почему рейтинги в процентах это бред рейтинги в балах, процент чего крутизины блин?
   - В `web/js/pages/overview.js` убран знак % у числового значения score в таблице топ-прокси.
   - Рейтинг теперь отображается в баллах, а не как процент.

2. [DONE] Logs System logs and activity вообще не заполнен там надо нормально сделать с фильтрами сортировками и все такое нормальный такой контроль логов
   - `hunt/logging_config.py` теперь пишет логи в `data/huntproxy.log` в дополнение к stdout.
   - `web/js/pages/logs.js` добавлены переключатели "Сначала новые" / "Авто-прокрутка" и авто-скролл к самым свежим строкам.
   - Добавлены i18n-ключи `reverse` и `autoScroll` в `web/locales/en.json` и `ru.json`.

3. [DONE] Analytics Proxy performance analytics and trends там явно с графиками чтото не так толстенные линии как будто они отмасштабированны и вообще оцени их адрекватность и приведи в порядок
   - В `web/js/charts.js` линии графиков получили `vector-effect="non-scaling-stroke"` и толщина уменьшена до 1.5px, чтобы линии не казались толстыми при масштабировании SVG.
   - В `web/js/pages/analytics.js` кастомные SVG-графики (pool size, bandwidth) также используют `vector-effect="non-scaling-stroke"` и толщину 1.5px.

4. [DONE] Connectivity Event Log постоянно стирается хотя должен хранить всю историю
   - `hunt/snapshot.py` `get_activity()` теперь читает события из SQLite (`events` таблица), а не только из in-memory списка, поэтому история сохраняется между сессиями/перезапусками.
   - `web/js/pages/connectivity.js` лог событий теперь загружается из `/api/activity` каждые 10 секунд, фильтруя события internet/ISP/canary.
   - Фронтенд больше не ведет отдельный in-memory eventLog — источник правды бэкенд.

5. [DONE] Логотип надо сделать кликабельным на главную
   - В `web/index.html` логотип в `sidebar-brand` обернут в ссылку, которая вызывает `router.navigate('overview')`.

6. [DONE] в Proxies нет SSL надо добавить не работает быстрой прокси
   - В `web/js/pages/proxies.js` добавлена колонка SSL в таблицу прокси, в поиск добавлен фильтр по `ssl`.
   - Кнопка "Sel" теперь не только выбирает апстрим, но и автоматически запускает локальный прокси, если он не запущен (аналогично Overview).
   - В `hunt/server.py` исправлено группирование по протоколу: HTTPS теперь определяется по `ssl_supported` или `protocol === 'https'`, а не только по `supports_connect`.
   - В `web/js/pages/overview.js`, `web/js/pages/hunt.js` и legacy-страницах `hunt/server.py` флаг HTTPS теперь показывается для всех SSL-прокси, включая те, что не поддерживают CONNECT.

7. [DONE] за SSL надо давать плюсы как и за скорость
   - В `hunt/models.py` в формулу score добавлен бонус: `+10` за `ssl_supported`, дополнительно `+5` за `supports_connect`.
   - Добавлен тест `test_score_bonus_for_ssl` в `tests/test_proxy_rating.py`.

----
Все тесты проходят: 143 passed.
